import React from "react"
import { Image } from "gatsby"

const Hero = class extends React.Component {
    constructor(props) {
        super(props)
        this.state = {}
    }

    render () {
        return (
            <div>
                I'm a hero.
            </div>
        )
    }
}

export default Hero